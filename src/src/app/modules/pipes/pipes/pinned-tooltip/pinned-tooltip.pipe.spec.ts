import {PinnedTooltipPipe} from './pinned-tooltip.pipe';

describe('PinnedTooltipPipe', () => {
  it('create an instance', () => {
    const pipe = new PinnedTooltipPipe();
    expect(pipe).toBeTruthy();
  });
});
