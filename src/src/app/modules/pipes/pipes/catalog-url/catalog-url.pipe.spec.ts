import {CatalogUrlPipe} from './catalog-url.pipe';

describe('CatalogUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new CatalogUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
